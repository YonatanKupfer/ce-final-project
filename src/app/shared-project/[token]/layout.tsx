import "@/app/globals.css";

export const metadata = {
    title: "צפייה בפרויקט - פרויקטי גמר",
};

export default function SharedProjectLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
