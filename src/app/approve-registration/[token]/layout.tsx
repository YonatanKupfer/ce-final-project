import "@/app/globals.css";

export const metadata = {
    title: "אישור/דחיית הרשמה - פרויקטי גמר",
};

export default function ApproveRegistrationLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
